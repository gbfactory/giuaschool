{% extends 'tema-new/layout-base.html.twig' %}

{% block pagina_contenuto %}
<div class="container-fluid">
  {{ include('tema-new/include/page-header.html.twig', {title: titolo|trans, actions: [{label: 'label.add'|trans, url: path('scuola_moduliFormativi_edit'), title: 'Nuovo modulo formativo'}]}) }}
  {{ include('tema-new/include/flash.html.twig') }}
  <div class="table-responsive">
    <table class="table table-bordered table-hover table-striped table-sm gs-data-table">
      <thead class="table-light">
        <tr>
          <th class="col-auto" scope="col">{{ 'label.nome'|trans }}</th>
          <th class="col-auto" scope="col">{{ 'label.tipo'|trans }}</th>
          <th class="col-auto" scope="col">{{ 'label.classi'|trans }}</th>
          <th class="col-auto" scope="col">{{ 'label.action'|trans }}</th>
        </tr>
      </thead>
      <tbody>
  {% for m in dati %}
        <tr>
          <td>{{ m.nomeBreve }}</td>
          <td>{{ ('label.modulo_formativo_tipo_'~m.tipo)|trans }}</td>
          <td><small>{% for c in m.classi %}{{ ('label.classi_'~c)|trans }}{{ not loop.last ? ', '}}{% endfor %}</small></td>
          <td class="pb-0">
            <button type="button" class="btn btn-primary btn-xs me-1 mb-1" title="Mostra i dettagli del modulo formativo" data-bs-toggle="modal" data-bs-target="#gs-modal-info" data-titolo="{{ m.nome }}" data-cont-id="info-moduloFormativo-{{ m.id }}">{{ 'label.show'|trans }}</button>
            <a class="btn btn-primary btn-xs me-1 mb-1" href="{{ path('scuola_moduliFormativi_edit', {'id': m.id}) }}" role="button" title="Modifica i dati del modulo formativo">{{ 'label.edit'|trans }}</a>
            <button type="button" class="btn btn-danger btn-xs me-1 mb-1 gs-modal-confirm-button" title="Cancella il modulo formativo" data-bs-toggle="modal" data-bs-target="#gs-modal-confirm" data-titolo="{{ m.nome }}" data-contenuto="{{ 'message.cancella_moduloFormativo'|trans }}" data-href="{{ path('scuola_moduliFormativi_delete', {'id': m.id}) }}">{{ 'label.delete'|trans }}</button>
            <div class="d-none" id="info-moduloFormativo-{{ m.id }}">
              <dl class="row">
                <dt class="col-sm-3 text-end">{{ 'label.nome'|trans }}</dt>
                <dd class="col-sm-9">{{ m.nome }}</dd>
                <dt class="col-sm-3 text-end">{{ 'label.nome_breve'|trans }}</dt>
                <dd class="col-sm-9">{{ m.nomeBreve }}</dd>
                <dt class="col-sm-3 text-end">{{ 'label.tipo'|trans }}</dt>
                <dd class="col-sm-9">{{ ('label.modulo_formativo_tipo_'~m.tipo)|trans }}</dd>
                <dt class="col-sm-3 text-end">{{ 'label.classi'|trans }}</dt>
                <dd class="col-sm-9">{% for c in m.classi %}{{ ('label.classi_'~c)|trans }}{{ not loop.last ? ', '}}{% endfor %}</dd>
              </dl>
            </div>
          </td>
        </tr>
  {% endfor %}
      </tbody>
    </table>
  </div>
  {{ include('tema-new/include/modal-info.html.twig') }}
  {{ include('tema-new/include/modal-confirm.html.twig') }}
</div>
{% endblock %}

{% block pagina_js_fine %}
<script>
$(document).ready(function() {
  document.getElementById('gs-modal-info').addEventListener('show.bs.modal', function (e) {
    var titolo = $(e.relatedTarget).data('titolo');
    var contenuto = $('#'+$(e.relatedTarget).data('cont-id')).html();
    $(this).find('#gs-modal-info-titolo').html(titolo);
    $(this).find('#gs-modal-info-contenuto').html(contenuto);
  });
  document.getElementById('gs-modal-confirm').addEventListener('show.bs.modal', function (e) {
    var titolo = $(e.relatedTarget).data('titolo');
    var contenuto = $(e.relatedTarget).data('contenuto');
    var href = $(e.relatedTarget).data('href');
    $(this).find('#gs-modal-confirm-titolo').html(titolo);
    $(this).find('#gs-modal-confirm-contenuto').html(contenuto);
    $(this).find('#gs-modal-confirm-yes').attr('href', href);
  });
});
</script>
{% endblock %}
